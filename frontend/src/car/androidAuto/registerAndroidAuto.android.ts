export const registerAndroidAuto = async () => {
  // Intentionally disabled in current runtime to prevent Expo Go / dev tunnel crashes.
  // Android Auto native runtime should be enabled only in a dedicated development build branch.
  return;
};
